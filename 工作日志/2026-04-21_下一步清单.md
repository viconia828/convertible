# 下一步清单

## 近期

- [x] 将“新增模块或新增功能前先落设计文档，再开始代码实现”补充为项目级规则
- [x] 为“统一缓存层”先落设计文档，再开始代码重构
- [x] 确认因子打分导出字段采用“展示 sheet + diagnostics sheet”分层方案
- [x] 完成两轮 `YTM` / 缓存链路性能优化，并完成真实样本复核
- [x] 完成统一缓存层第一阶段骨架
- [x] 将 `DataLoader` / `TradingCalendar` / `scoring_exports` 接入统一缓存层
- [x] 为统一缓存层补直接测试，并完成回归验证
- [x] 完成 `cb_rate` 按缺失 `ytm` 代码按需加载优化，并将缓存 stats 接入 benchmark
- [x] 完成 `cb_daily_cross_section` 按列读取优化，并让因子打分链路只读取最小必要列
- [x] 完成 `cb_daily_cross_section` 的 `factor_history_v1` 月度聚合缓存，并接入因子打分链路
- [ ] 为统一缓存层补更完整的观测指标：
  - hit / miss / partial hit
  - 文件扫描数
  - 远端补数次数
  - 回写次数
  - 分阶段耗时
- [ ] 评估是否将 `credit_spread` 的 metadata 管理进一步收口到缓存层
- [ ] 评估是否将 `factor_history_v1` 扩展为更多稳定聚合 profile
- [ ] 评估是否将 `ytm` 等派生值独立为 derived cache，而不是继续直接回写原缓存
- [x] 完成请求级 panel cache 评估，确认建议先做统一缓存层内的 runtime-only request panel cache
- [x] 补 `cb_daily_cross_section` 写回 / 聚合更新到更高层缓存的粗粒度失效链
- [x] 在统一缓存层实现 `cb_daily_cross_section + factor_history_v1` 的请求级 panel cache，并补命中 / 失效统计
- [x] 为环境打分增加自动识别 warm-up 区间，并在导出层自动跳过预热交易日
- [x] 修正环境打分 warm-up 导出口径：将“导出预热观察数”和内部 `fully_ready` 诊断拆开，确保预热充足时从请求首日开始导出
- [x] 为环境导出补上 `credit_spread` 覆盖不足时的自动补数，并将 warm-up 计数修正为“请求首日前完整 20 个交易日预热”
- [x] 将环境导出中 pre-ready 的 `trend_strength` 改为空值显示，并在 summary 中补充 `trend_first_ready_date`
- [x] 先落 `strategy` 模块骨架设计文档，明确 `StrategyService -> StrategyEngine -> PortfolioBuilder` 分层与第一阶段实现边界
- [x] 按设计文档新增 `strategy/` 第一阶段骨架：
  - `__init__.py`
  - `snapshot.py`
  - `result.py`
  - `portfolio.py`
  - `engine.py`
  - `service.py`
- [x] 在 [strategy_config.py](C:/Users/ai/Desktop/可转债多因子/config/strategy_config.py) 中补最小 `strategy` 参数区，并同步更新 `策略参数.txt`
- [x] 将环境/因子的历史窗口解析从导出层抽成共享 helper，避免 `strategy` 再复制一套口径
- [x] 为 `strategy` 第一阶段补纯函数、无未来函数、权重约束与空池回退测试
- [x] 为 `strategy` 补一个轻量预览入口，支持单日期输出环境、因子权重和目标组合
- [ ] 评估是否为 `strategy` 预览入口补“指定候选代码 / 指定持仓观察名单”视角，减少全市场结果阅读成本
- [x] 评估并落地让 `StrategyService` / `strategy` 预览复用环境/因子导出层已有 notes / summary 口径，避免继续各自维护提示文本
- [ ] 评估是否继续把环境 `warmup_first_ready_date` / `trend_first_ready_date` 一类解析也抽成共享 helper，避免导出层继续独占这部分时序口径
- [ ] 评估是否给 `strategy` 预览补一个“summary only / verbose”开关，避免当前信息量对日常实跑偏重
- [ ] 评估是否需要把环境层的 warm-up readiness 诊断单独导出到 diagnostics 或 summary 扩展项
- [ ] 评估是否需要在环境 summary 中同时暴露“导出 warm-up 首日”和“内部 fully-ready 首日”，避免后续诊断口径混淆
- [ ] 评估是否需要把 `equity_strength` / `bond_strength` 也改为在 pre-ready 阶段留空，而不是继续显示由默认值平滑得到的早期数值
- [ ] 评估是否需要在环境导出 summary / notes 中显式暴露 “credit_spread 自动补数失败并 fallback_local” 的告警，避免网络失败时结果看起来像正常预热
- [ ] 评估是否需要在因子 diagnostics / summary 中显式突出“交易日但单债 `not_tradable`”的说明，减少和“非交易日”混淆
- [ ] 评估是否将 request panel cache 扩展到更多稳定 profile
- [ ] 评估是否让 request panel cache 进一步服务 `strategy` 主链路

## 中期

- [ ] 将 `DataCacheService` 继续按职责拆分为 `observer / coverage / writeback / policy / models`
- [x] 将缓存 stats 接入 benchmark 工具，形成统一性能观察口径
- [ ] 评估是否需要加入单次运行内的内存缓存，减少重复 `read_csv`
- [x] 为聚合缓存增加轻量运行期内存缓存，并将命中统计接入 benchmark
- [ ] 为缓存层增加 schema / 口径版本治理，支持后续策略模块稳定接入

## 长期

- [x] 在统一缓存层稳定后，再开始 `strategy` 模块骨架设计与落档
- [ ] 为后续 `strategy` 回测和组合构建预留更稳定的缓存预热与失效机制

