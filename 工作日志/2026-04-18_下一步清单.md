# 下一步清单

## 近期

- [x] 阅读模板并完成根目录环境初始化
- [x] 阅读两份主文档并按更新时间识别优先级
- [x] 建立工作日志目录与初始化日志
- [x] 向用户确认当前理解的开发顺序与第一步拆解
- [x] 在确认后开始 `Step 0：data 接口层`
- [x] 产出 `DataSchema`、`DataLoader`、`TradingCalendar` 初版接口
- [x] 为转债基础信息增加固定字段与可变字段缓存拆分
- [x] 接入 Tushare `trade_cal` 并支持本地交易日历缓存
- [x] 补充第一步对应的基础测试文件
- [x] 运行本地测试并修正实现细节
- [x] 做一次通过 `DataLoader` 的真实 Tushare 日历 smoke test
- [x] 评估并补充下一批 Tushare 字段映射（转债扩展字段与宏观字段）
- [x] 明确 `premium_rate` / `ytm` / 大部分宏观指标 的数据来源方案
- [x] 实现 `env` 模块初版与测试
- [x] 落地 `factor` 模块，完成五因子初版与基础可投资池过滤
- [x] 落地 `model` 模块，完成 baseline 权重映射初版
- [x] 收口 `credit_spread` 当前阶段方案：本地参考表兜底，不阻塞主线
- [x] 准备 `data/local_reference/macro/credit_spread.csv` 的真实参考数据
- [x] 基于真实 `credit_spread` 参考数据补齐环境 smoke test
- [x] 为 `macro/env` 链路正式收口多源日期对齐策略
- [x] 为 `credit_spread` 实现断源时的本地回退与状态元数据机制
- [x] 为 `credit_spread` 留出主源/备用源可插拔接口
- [ ] 搭建 `strategy` 模块骨架与纯函数入口
- [ ] 设计并实现 `snapshot -> env/factors/weights` 的适配层
- [ ] 实现 `scoring`：五因子加权总分
- [ ] 实现 `portfolio_build`：`top_n`、等权/softmax、单债上限、归一化
- [ ] 实现 `strategy_fn` / `StrategyEngine`：单日输出目标组合
- [ ] 为 `strategy` 模块补纯函数、幂等、无未来函数测试

## 长期待办

- [ ] 为 `credit_spread` 补充非中国债券信息网的备用源，并持续保留在清单中直到解决
## 近期

- [x] 将 baseline 与当前主流程参数统一抽离到根目录 `策略参数.txt`
- [x] 新增 `strategy_config.py`，让 `data / env / factor / model` 默认读取统一参数
- [x] 新增环境打分独立导出入口：`tools/export_environment_scores.py` + `环境打分.bat`
- [x] 新增因子打分独立导出入口：`tools/export_factor_scores.py` + `因子打分.bat`
- [x] 为导出入口补充 XLSX 写出测试与参数加载测试
- [ ] 进入 `strategy` 模块前，确认因子打分导出字段是否还需要补充“更多原始因子诊断列”或“用户更精简的展示列”
- [ ] 搭建 `strategy` 模块骨架与纯函数入口
- [ ] 设计并实现 `snapshot -> env/factors/weights` 的适配层
- [ ] 实现 `scoring`：五因子加权总分
- [ ] 实现 `portfolio_build`：`top_n`、等权 / softmax、单债上限、归一化
- [ ] 实现 `strategy_fn` / `StrategyEngine`：单日输出目标组合
- [ ] 为 `strategy` 模块补纯函数、幂等、无未来函数测试

## 长期待办

- [ ] 为 `credit_spread` 补充非中国债券信息网的备用源，并持续保留在清单中直到解决
## 近期

- [x] 定位并修复 `环境打分.bat` 导出过慢、看起来无法出表的问题
- [x] 为环境打分导出增加“当前运行内 Tushare 失败后短路 + 本地缓存兜底”机制
- [x] 将 `meta / data / exports` 从 `策略参数.txt` 迁回代码默认配置
- [x] 将 `策略参数.txt` 收口为仅保留 `env / factor / model`，并补全中文注释
- [ ] 评估是否需要为 `因子打分` 入口进一步增加“显式缓存模式 / 强制刷新模式”两个用户选项
- [ ] 进入 `strategy` 模块前，确认因子打分导出字段是否还需要补充“更多原始因子诊断列”或“用户更精简的展示列”
- [ ] 搭建 `strategy` 模块骨架与纯函数入口
- [ ] 设计并实现 `snapshot -> env/factors/weights` 的适配层
- [ ] 实现 `scoring`：五因子加权总分
- [ ] 实现 `portfolio_build`：`top_n`、等权 / softmax、单债上限、归一化
- [ ] 实现 `strategy_fn` / `StrategyEngine`：单日输出目标组合
- [ ] 为 `strategy` 模块补纯函数、幂等、无未来函数测试
