# 下一步清单

## 近期

- [x] 诊断 `环境打分` 请求区间被截断到 `2026-03-02` 之后的问题
- [x] 修正 `cb_equal_weight` 的断档缓存覆盖判断
- [x] 单独定位 Tushare 连通性问题
- [x] 修正阻断型本地代理导致的 Tushare 连接失败
- [x] 为环境/因子导出入口补充代理风险提示
- [x] 回补 `2025-01-01 ~ 2026-04-01` 所需的 `cb_equal_weight / cb_daily_cross_section` 历史缓存
- [x] 复跑 `环境打分` 并确认 `2026-01-05 ~ 2026-04-01` 可正常出表
- [x] 为 `因子打分` 增加 `run_summary` 中文注释、实际输出区间与“无可用因子分数”提示
- [x] 明确否决“显式缓存模式”作为正式用户功能，统一改为“默认缓存优先 + 可选强制刷新”
- [x] 为环境打分与因子打分统一增加 `fetch_policy / refresh_requested / data_quality_status`
- [x] 将“缓存优先、完整性优先、数据不完整必须警告”的规则写入总设计文档与开发路线
- [x] 将“是否强制刷新”的默认值下沉到 `策略参数.txt`，交互入口不再单独询问
- [x] 为环境打分首次补缓存增加 `cb_daily_cross_section` 缺失交易日并发抓取优化
- [x] 为因子打分首次补缓存增加 `cb_daily / cb_rate` 缺失代码并发抓取优化
- [x] 修正因子打分“单码导出时误在请求子集上做截面打分”的逻辑错误
- [x] 将因子打分改为“全市场截面 + 批量面板计算 + 最后裁切请求代码”
- [x] 将因子打分导出改为“每个代码一个 sheet”
- [x] 将因子打分预热历史改为“动态推荐窗口 + 配置上限封顶”
- [x] 为项目补根目录 `README.md`，面向协作开发者说明总目标、当前进度与接手方式
- [ ] 进入 `strategy` 模块前，确认因子打分导出字段是否需要补充“更多原始因子诊断列”或改为“更精简的用户展示列”
- [ ] 评估是否需要进一步优化批量因子计算中的 YTM 估值与内存占用
- [ ] 评估是否需要进一步压缩环境打分的预热窗口，减少首次补缓存的历史取数范围
- [ ] 评估是否需要把 `cb_daily_cross_section` 从按日 CSV 缓存升级为更高效的聚合缓存格式
- [ ] 评估是否需要把“代理诊断提示”进一步下沉到更多脚本入口或数据层异常信息中
- [ ] 搭建 `strategy` 模块骨架与纯函数入口
- [ ] 设计并实现 `snapshot -> env/factors/weights` 的适配层
- [ ] 实现 `scoring`：五因子加权总分
- [ ] 实现 `portfolio_build`：`top_n`、等权 / softmax、单债上限、归一化
- [ ] 实现 `strategy_fn` / `StrategyEngine`：单日输出目标组合
- [ ] 为 `strategy` 模块补纯函数、幂等、无未来函数测试

## 长期待办

- [ ] 为 `credit_spread` 补充非中国债券信息网的备用源，并持续保留在清单中直到解决
