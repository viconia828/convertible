# 下一步清单

## 近期

- [x] 先落 `factor_history_v1` 扩展评估方案文档，再开始真实样本评估
- [x] 用现有 `benchmark_factor_export.py` 复核 `factor_history_v1` 当前收益上限，形成 cold / hot / overlap 基线
- [x] 复核 `strategy` 单日期主链，确认当前收益主要来自 runtime snapshot reuse，而不是第二个横截面 profile
- [x] 复核环境链里的 `cb_equal_weight` 重建路径，确认它当前不构成第二个稳定高频候选
- [x] 基于真实样本复核，确认当前阶段暂不新增第二个 `cb_daily_cross_section` aggregate profile
- [x] 将 README / 总清单里的主优先级切换到 `DataCacheService` 拆分和 schema / 口径版本治理
- [x] 先落 `DataCacheService` 职责拆分方案文档，再开始代码重排
- [x] 完成 `DataCacheService` 第一阶段职责拆分：`observer / coverage / writeback / policy / models`
- [x] 先落缓存 schema / 口径版本治理方案文档，再开始代码实现
- [x] 完成缓存 schema / 口径版本治理第一版：`coverage.json + aggregate .meta.json`
- [x] 先落 `strategy` runtime snapshot 与底层缓存一致性方案文档，再开始代码实现
- [x] 完成 `strategy` runtime snapshot 的同实例修订号失效保护
- [x] 先落环境历史缓冲动态收缩方案文档，再开始代码实现
- [x] 完成环境导出历史缓冲的动态收缩：`fully-ready` 需求 + cap
- [x] 评估并收口 `StrategyService` snapshot 历史起点的动态缓冲语义：支持自动模式 + 可选手工扩宽
- [x] 结合批量回测 / 批量预览日期调度完成默认历史缓冲切换评估，结论为当前不把 `strategy.history_buffer_calendar_days` 默认切到 `0`

## 中期

- [ ] 为批量 preview / backtest 设计共享日期调度 helper，并显式区分 `fully-ready` 最小窗口与环境稳定评分窗口
- [ ] 为后续 `strategy` 回测和组合构建继续收口更稳定的缓存预热窗口约束
- [ ] 评估是否需要把当前“全局保守失效”进一步缩窄到 dataset / month 级别

## 长期

- [ ] 为后续 `strategy` 回测和组合构建预留更稳定的缓存预热与失效机制
