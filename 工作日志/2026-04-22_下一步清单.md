# 下一步清单

## 近期

- [x] 先落 `strategy` 预览观察名单视角方案文档，再开始代码实现
- [x] 为 `strategy` 单日期预览入口补“指定候选代码 / 观察名单”视角，减少全市场结果阅读成本
- [x] 先落 `strategy` 预览 `summary only / verbose` 开关方案文档，再开始代码实现
- [x] 为 `strategy` 预览补 `summary only / verbose` 双视图，降低日常实跑的信息负担
- [x] 先落环境 ready 时序共享 helper 方案文档，再开始代码实现
- [x] 将环境 `warmup_first_ready_date` / `trend_first_ready_date` 一类解析收口成共享 helper，避免导出层继续独占这部分时序口径
- [x] 先落 `strategy snapshot` 运行期复用方案文档，再开始代码实现
- [x] 让 request panel cache 通过 `StrategyService` 的 runtime snapshot reuse 更直接服务到 `strategy` 主链路
- [x] 先落统一缓存层观测指标方案文档，再开始代码实现
- [x] 为统一缓存层补更完整的观测指标：
  - hit / miss / partial hit
  - 文件扫描数
  - 远端补数次数
  - 回写次数
  - 分阶段耗时
- [x] 先落缓存诊断展示方案文档，再开始代码实现
- [x] 在预览或 benchmark 入口上补更统一的缓存诊断展示，验证 runtime snapshot / panel / aggregate 三层收益
- [x] 先落 `strategy` 单交易日观察导出方案文档，再开始代码实现
- [x] 为 `strategy` 补单交易日观察导出入口，支持批处理交互和 xlsx 留档
- [x] 归档根目录散落的通用 `.py` 模块，将配置 / 共享 helper / 导出层分别收口到 `config/`、`shared/`、`exports/`
- [x] 基于新的 preview / benchmark 缓存诊断，评估是否将 `factor_history_v1` 扩展为更多稳定聚合 profile
- [ ] 如果后续回测主链出现新的稳定横截面列投影，再单独复评是否新增第二个 aggregate profile

## 中期

- [ ] 将 `DataCacheService` 继续按职责拆分为 `observer / coverage / writeback / policy / models`
- [ ] 为缓存层增加 schema / 口径版本治理，支持后续策略模块稳定接入

## 长期

- [ ] 为后续 `strategy` 回测和组合构建预留更稳定的缓存预热与失效机制
